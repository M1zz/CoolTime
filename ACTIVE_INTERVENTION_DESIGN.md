# ③ 능동 개입 설계 — "지를 때 먼저 말려주기"

> 위젯(글랜스)은 수동적이다. 진짜 한 방은 **충동의 순간에 능동적으로 개입**하는 것.
> 이 문서는 iOS에서 가능한 범위를 정직하게 정리하고, 구현 가능한 설계를 제시한다.

## 0. iOS의 벽 (먼저 기대치 설정)

- ❌ 앱이 "사용자가 배달앱을 열었다"를 **직접 감지/가로채기 불가.** (백그라운드 앱 모니터링 API 없음)
- ✅ 하지만 **단축어(Shortcuts) 개인용 자동화의 "앱을 열 때" 트리거**는 사용자가 직접 설정하면 동작한다 → 이게 유일하고 합법적인 "앱 열 때 개입" 경로.
- ✅ **App Intents**로 "지금 사도 돼?"·"샀어요"를 Siri/단축어/위젯 버튼에 노출 가능.
- ✅ **인터랙티브 위젯 버튼**(iOS17+): 앱 안 열고 위젯에서 바로 "샀어요" 기록.
- ✅ **Live Activity / Dynamic Island**: 가장 임박한 쿨타임을 잠금화면 상단에 상주.

→ 결론: **"앱 열면 개입"은 우리가 강제 못 하고, 사용자가 단축어 자동화로 켜야 한다.** 우리의 일은 (1) 그 자동화를 1탭으로 깔게 돕고 (2) 트리거됐을 때 강력한 개입 화면을 주는 것.

---

## 1. 핵심 구성요소

### A. App Intents (가장 중요)
공유 코드(앱+위젯이 함께 쓰는 `CooldownItem` 접근)로 Intent 정의.

1. **`CheckUrgeIntent` ("지금 사도 돼?")**
   - 파라미터: 항목(선택, 없으면 가장 돈 걸린 대기 항목).
   - 반환/표시: `IntentDialog` + 커스텀 스니펫 뷰 →
     "🛍️ 온라인 쇼핑 — **아직 10일 남았어요.** 지금 사면 ₩50,000. 이번 달 벌써 ₩305,000 아꼈어요. 그래도 살까요?"
   - 버튼: **[참을게요]** (그냥 닫기, 스트릭 유지) / **[샀어요]** (`LogPurchaseIntent` 호출).
2. **`LogPurchaseIntent` ("샀어요 기록")**
   - 해당 항목 `use()` 호출 → 쿨타임 재시작, 깸 기록.
   - `.openAppWhenRun = false` (앱 안 열고 백그라운드 처리) + 확인 다이얼로그.
3. **`AppShortcutsProvider`** 로 위 인텐트를 시스템에 등록 → Spotlight/Siri에서 "사도 돼?" 음성 호출 가능, 단축어 갤러리에 자동 노출.

### B. 단축어 개인용 자동화 (앱 열 때 개입)
- 사용자가 만드는 자동화: **"[배달앱/쿠팡/무신사]를 열 때 → CheckUrgeIntent 실행."**
- iOS 자동화는 "즉시 실행" 옵션을 켜면 묻지 않고 바로 뜬다(iOS 16.4+에서 일부 트리거 자동 실행 가능; 앱 열기 트리거는 보통 배너→탭 필요).
- **현실적 UX**: 앱 열면 상단에 "CoolTime: 아직 10일" 배너 → 탭하면 개입 스니펫. 완벽한 차단은 아니지만 "한 박자"는 만든다.

### C. 인앱 가이드 설치 (채택률의 관건)
자동화를 손으로 만들게 하면 아무도 안 한다. → **온보딩/설정에 "충동 앱 지정" 단계**:
- "어떤 앱에서 자주 지르나요?" 멀티선택(배달의민족/쿠팡/무신사/…)
- 각 선택마다 **단축어 자동화 딥링크/가이드** 제공. (iOS는 자동화 자동생성 API가 없어 100% 자동은 불가 → 단계별 스크린샷 가이드 + `shortcuts://` 딥링크로 최대한 가깝게.)

### D. 인터랙티브 위젯 버튼 (보조)
- 위젯의 히어로 항목에 **[샀어요]** 버튼(`LogPurchaseIntent`) → 앱 안 열고 1탭 기록.
- "참는 중"일 때 버튼 라벨은 회색 "아직 N일"(비활성 느낌)로, 누르면 확인.

### E. Live Activity (선택, 임팩트 큼)
- 가장 임박한 쿨타임을 Live Activity로 → 잠금화면/다이내믹 아일랜드에 "🛍️ 아직 2일" 상주.
- 지를까 폰 들 때마다 눈에 들어옴 = 수동 위젯보다 강한 글랜스.

---

## 2. 아키텍처 / 작업

- **App Intents 타겟 공유**: 인텐트가 SwiftData(`CooldownItem`)에 접근해야 함 → ModelContainer를 App Group 컨테이너로 옮겨 앱·인텐트·위젯이 같은 store 공유. (현재는 위젯이 UserDefaults 스냅샷만 읽음 → 인텐트로 "기록"하려면 공유 SwiftData 필요.)
  - 대안(가벼움): 인텐트는 공유 UserDefaults에 "구매 이벤트"를 큐로 적고, 앱이 다음 실행 때 반영. 단순하지만 즉시성↓.
  - 권장: **App Group SwiftData 공유**로 일관성 확보.
- **신규 파일**: `Intents/CheckUrgeIntent.swift`, `Intents/LogPurchaseIntent.swift`, `Intents/CoolTimeShortcuts.swift`, 인텐트 스니펫 뷰.
- **위젯**: 버튼을 `Button(intent:)`로 교체(iOS17 widget interactivity).
- **앱**: 설정/온보딩에 "충동 앱 지정 + 자동화 설치 가이드" 화면.

## 3. 단계별 (작은 것부터, 각각 독립 출시 가능)

1. **App Intents 2종 + AppShortcuts** — Siri/Spotlight "사도 돼?" 가능. (중)
2. **인터랙티브 위젯 [샀어요] 버튼** — 공유 SwiftData 전환 포함. (중상)
3. **인앱 자동화 가이드** — "앱 열 때 개입" 사용자 설치 플로우. (중, UX 카피가 핵심)
4. **Live Activity** — 임박 쿨타임 상주. (상)

## 4. 리스크 / 정직한 한계

- **"앱 열면 자동 차단"은 불가능.** 사용자가 자동화를 깔아야 하고, 그마저 배너 탭이 낀다. → 마케팅에서 과장 금지.
- App Group **SwiftData 공유 전환**이 가장 큰 기술 부채(마이그레이션 주의). 가벼운 UserDefaults 큐로 시작해도 됨.
- 자동화 설치 이탈률이 높을 것 → **3번(가이드 UX)** 이 사실상 이 기능의 성패. 여기에 카피·동선 투자 필요.

## 5. 추천 시작점

**1번(App Intents) → 2번(위젯 버튼)** 부터. 코드량 대비 체감이 크고, "사도 돼?"를 Siri로 부를 수 있게 되는 순간 제품의 성격이 '기록장'에서 '개입자'로 바뀐다. 3·4는 그다음.
